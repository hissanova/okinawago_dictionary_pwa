import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.okinawago.app',
  appName: 'okinawago_dictionary_pwa',
  webDir: 'build'
};

export default config;
